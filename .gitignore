# Secrets & local config (never commit) — see SETUP.md
.slack-bot-token
*.token
settings.local.json

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp

# Python
__pycache__/
*.pyc

# Superpowers working files must never be tracked (global rule)
docs/superpowers/

# Externally-managed marketplace plugins installed into this skills dir
# (their own repos — install/update from their marketplaces, don't vendor here)
ui-ux-pro-max-skill/

# ── gstack ──────────────────────────────────────────────────────────────
# gstack is installed separately (README step 3) into this dir; its skills
# sync into the top-level folders below. None of it belongs in this repo.
# If gstack adds new skills, append them here (or regenerate this block).
#
# This block shrinks as we take skills over — see docs/degstack.md. A skill
# removed from here is ours now, tracked, and gstack's copy of it is dead.
# `review/` was the first to go.
gstack/
_gstack-command/
autoplan/
benchmark/
benchmark-models/
canary/
connect-chrome/
cso/
design-consultation/
design-html/
design-review/
design-shotgun/
devex-review/
document-generate/
document-release/
gstack-upgrade/
health/
ios-clean/
ios-design-review/
ios-fix/
ios-qa/
ios-sync/
land-and-deploy/
landing-report/
office-hours/
open-gstack-browser/
pair-agent/
plan-ceo-review/
plan-design-review/
plan-devex-review/
plan-eng-review/
plan-tune/
qa/
qa-only/
retro/
setup-browser-cookies/
setup-deploy/
setup-gbrain/
skillify/
sync-gbrain/

# ── build output of the vendored TS skills ──────────────────────────────
# browse/ and make-pdf/ ship compiled binaries. The binaries are kept on disk
# so the skills keep working, but they are build output and 275M of it, so
# they stay untracked. Their build tooling lived in gstack; see the note in
# browse/SKILL.md before changing their source.
browse/dist/
browse/test/fixtures/
make-pdf/dist/
*/node_modules/
