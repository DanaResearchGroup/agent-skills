# SESSIONS — herdr session registry (workspace `{{HERDR_WORKSPACE}}`)

Who is doing what, which ticket, current status. Pulled live via herdr's machine-readable
output + the event log; `bin/reconcile` renders the table. Tab naming: `CONVENTIONS.md` §5.

<!-- GENERATED:BEGIN sessions -->
(run bin/reconcile to populate from live herdr state)
<!-- GENERATED:END sessions -->

---

## Notes (hand-authored, kept across reconcile)

(free-form operator notes about sessions go here, outside the generated block)
