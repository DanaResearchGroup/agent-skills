# WORKTREES — live branch/worktree registry (narrative surface)

Dispositions: KEEP (active) · LIVE (concurrent session — hands off) · CLEANUP (merged,
remove worktree + branch) · DECIDE (unmerged content, needs a call) · DORMANT (old line).
Naming for NEW branches/worktrees: `CONVENTIONS.md` §7.

> `bin/reconcile` rebuilds the per-repo tables below from live `git worktree list` +
> `pm_git_probe` across the configured repos. Hand-typed notes OUTSIDE the markers are kept.

<!-- GENERATED:BEGIN worktrees -->
(run bin/reconcile to populate from live git state across the configured repos)
<!-- GENERATED:END worktrees -->

---

## Notes (hand-authored, kept across reconcile)

(free-form operator notes about branches/worktrees go here, outside the generated block)
